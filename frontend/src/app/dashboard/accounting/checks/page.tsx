"use client";import{useEffect,useState}from"react";import Link from"next/link";import{formatDate,formatMoney}from"@/lib/money";import{listChecks,type CheckRecord}from"@/lib/checks";export default function ChecksPage(){const[rows,setRows]=useState<CheckRecord[]>([]),[error,setError]=useState(""),[from,setFrom]=useState(""),[to,setTo]=useState("");async function load(){try{setError("");setRows((await listChecks({date_from:from||undefined,date_to:to||undefined})).items)}catch(e){setError(e instanceof Error?e.message:"Could not load checks")}}useEffect(()=>{void load()},[]);return <div className="p-6"><div className="flex justify-between items-center"><div><h1 className="text-xl font-semibold">Checks</h1><p className="text-sm text-slate-500">Issued and voided checks.</p></div><Link href="/dashboard/accounting/checks/write" className="px-3 py-2 rounded bg-blue-600 text-white text-sm">Write Checks</Link></div><div className="my-5 flex gap-2 items-end"><label className="text-xs">From<input className="block border rounded p-2 text-sm" type="date" value={from} onChange={e=>setFrom(e.target.value)}/></label><label className="text-xs">To<input className="block border rounded p-2 text-sm" type="date" value={to} onChange={e=>setTo(e.target.value)}/></label><button onClick={()=>void load()} className="border rounded px-3 py-2 text-sm">Show</button></div>{error&&<p className="text-red-600">{error}</p>}<div className="border rounded bg-white overflow-hidden"><table className="w-full text-sm"><thead className="bg-slate-50"><tr><th className="p-3 text-left">Check</th><th className="p-3 text-left">Date</th><th className="p-3 text-left">Payee</th><th className="p-3 text-right">Amount</th><th className="p-3 text-left">Status</th></tr></thead><tbody>{rows.map(r=><tr key={r.id} className="border-t"><td className="p-3"><Link className="text-blue-600" href={`/dashboard/accounting/checks/${r.id}`}>{r.check_number}</Link></td><td className="p-3">{formatDate(r.check_date)}</td><td className="p-3">{r.payee_name}</td><td className="p-3 text-right">{formatMoney(r.amount)}</td><td className="p-3">{r.status}</td></tr>)}</tbody></table></div></div>}