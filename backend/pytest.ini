[pytest]
minversion = 9.0
addopts = -ra --strict-markers
pythonpath = .
testpaths = tests
markers =
    integration: exercises database/migration integration behavior
    accounting: verifies financial posting invariants
    e2e: real browser smoke test against running frontend/backend
